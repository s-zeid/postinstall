
module scottywz-php-fpm 1.0;

require {
	type httpd_t;
	type user_home_t;
	class dir add_name;
	class dir write;
	class file create;
        class file write;
}

#============= httpd_t ==============
allow httpd_t user_home_t:dir { add_name write };
allow httpd_t user_home_t:file { create write };
